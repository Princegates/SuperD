name: superd
description: "SuperD - free, open-source courier delivery management app"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Path-based URLs on web (superd.example.com/v/CODE instead of
  # superd.example.com/#/v/CODE) - needed so vendor/customer links are
  # clean and shareable. Ships with the Flutter SDK, no version to pin.
  flutter_web_plugins:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Backend: self-hostable, open-source Supabase (Postgres + Auth + Storage + Realtime)
  supabase_flutter: ^2.8.0

  # State management
  flutter_riverpod: ^2.6.1

  # Navigation
  go_router: ^14.8.1

  # Maps - Google Maps on Android/iOS/web. Needs a Google Cloud project
  # with billing enabled and an API key you provide yourself - see the
  # README's "Google Maps setup" section. latlong2 stays as the plain
  # LatLng value type used everywhere else in the app (models, providers,
  # repositories); only the two map-rendering widgets talk to
  # google_maps_flutter directly.
  google_maps_flutter: ^2.18.0
  latlong2: ^0.9.1

  # Device location for drivers
  geolocator: ^13.0.2

  # Proof-of-delivery photo capture
  image_picker: ^1.1.2

  # Deep-link to the device's native maps app for turn-by-turn navigation
  url_launcher: ^6.3.1

  # Date/time formatting
  intl: ^0.19.0

  # Reverse geocoding (coordinates -> a readable address) via OpenStreetMap's
  # free Nominatim service - no API key, and independent of which map
  # widget is showing the pin.
  http: ^1.2.0

  # Custom display/body fonts for the marketing welcome screen (Poppins/
  # Inter) - fetched once and cached on-device, no API key needed.
  google_fonts: ^6.2.1

  # Push notifications (Android/iOS) - see PushNotificationService and
  # the README's "Push notifications" section. Compiles and runs fine
  # with no Firebase project configured yet; every call degrades to a
  # no-op until google-services.json/GoogleService-Info.plist are added.
  firebase_core: ^3.15.0
  firebase_messaging: ^15.2.6

  # Crash/error reporting - see the README's "Crash reporting" section.
  # With no SENTRY_DSN set (the default), the SDK still initializes but
  # simply doesn't send anything anywhere.
  sentry_flutter: ^8.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates Android/iOS/web app icons from assets/icon/icon.png.
  # Run `dart run flutter_launcher_icons` after changing the source image.
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  image_path: "assets/icon/icon.png"
  android: true
  ios: true
  remove_alpha_ios: true
  web:
    generate: true
    background_color: "#FFFFFF"
    theme_color: "#16213E"
  android_min_sdk: 21
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/branding/logo_full.png
    - assets/icon/icon.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
