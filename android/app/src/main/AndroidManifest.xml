<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed for the driver app's live location sharing (Live Map) and
         the pickup/dropoff location picker. Without these, Android has
         nothing to prompt for and Geolocator.requestPermission() silently
         resolves without ever showing the permission dialog. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Lets a driver opt into "Allow all the time" so their live position
         keeps reaching dispatch/customers while the app is backgrounded or
         the phone is locked - see DriverDashboardScreen's
         _requestBackgroundPermissionIfPossible(). Android only shows the
         system prompt for this on first request; after that the driver has
         to grant it from Settings themselves - a platform restriction, not
         something requestable on demand. FOREGROUND_SERVICE(_LOCATION) is
         what lets background updates keep running at all past Android 9 -
         geolocator_android runs its own foreground service (with the
         required persistent notification) once this is granted, no extra
         <service> declaration needed here. Note for Play Store submission:
         declaring ACCESS_BACKGROUND_LOCATION requires filling out Google
         Play's separate "background location" disclosure in Play Console. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <application
        android:label="superd"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Google Maps API key - injected from android/local.properties via
             build.gradle.kts's manifestPlaceholders, never hardcoded here.
             See the README's "Google Maps setup" section. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
